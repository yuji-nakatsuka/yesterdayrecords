<%= render "layouts/admin_header"%>
			<h1>Label</h1>
			<p>削除すると、そのLabelが持つ全てのCDが削除されます。</p>

<table>
	<thead>
		<tr>
		</tr>
	</thead>
	<tbody>
		<% @labels.each do |label| %>
		<tr>
			<td><%= link_to label.label ,admins_sell_cds_label_show_path(label.id)%></td>
			<td><span><%= link_to "編集" , admins_sell_cds_label_edit_path(label.id)%></span>  /  <apan><%= link_to "削除", admins_dell_cds_label_destroy_path(label.id),method: :delete, data: { confirm: '削除すると、そのLabelが持つ全てのCDが削除されます。本当に削除しますか？'} %></apan></td>
		</tr>
		<% end %>
	</tbody>
</table>